# Project: 仿微信聊天 App

## Original Request
做一个仿微信聊天 app，本地先跑，后续再部署到服务器。移动端和网页端都能聊天同步，后台可管理用户、链接二维码、自动回复等。

## Confirmed Scope
第一阶段只做本地可运行版的登录 + 聊天骨架：
- 前后端分离。
- 网页端 + 后台 + H5 形态。
- SQLite 本地数据库。
- 手机号直接登录，无验证码。
- 私聊联系人、消息收发、未读数、在线状态、基础会话列表。

## Assumptions
- 本阶段不做真实短信、真实 App 打包、线上部署、对象存储。
- 文件上传、群聊、后台管理等在后续阶段逐步补。
- 代码放在当前项目目录下，不放 C 盘。

## Technical Approach
- `apps/server`：Node.js + Express + Socket.IO + SQLite。
- `apps/web`：Vite + React + TypeScript。
- `data/app.db`：本地 SQLite 数据库。
- `uploads/`：后续本地附件目录。

## Product Requirements
- User: 真实客户、管理员、客服。
- Goal: 本地先跑通聊天产品核心链路。
- MVP: 手机号登录、自动编号、联系人、私聊、消息实时同步。
- Success signal: 两个浏览器窗口用不同手机号登录后可以互相聊天同步。
- Out of scope: 上线部署、完整后台、群聊、附件上传。

## Plan
- [x] 创建本地项目结构和脚本。
- [x] 实现后端 API、SQLite 表、Socket.IO 实时消息。
- [x] 实现微信风聊天前端。
- [x] 本地启动并验证核心路径。

## Decisions
| Decision | Reason |
|---|---|
| 前后端分离 | 用户已选择，后续部署更清晰 |
| SQLite | 用户已选择，本地开发最快 |
| 微信绿工具风 | 用户已确认 UI 方向 |
| 当前目录下建 apps | 避免根目录散乱且不写入 C 盘 |
| Socket 监听只按用户绑定 | 避免切换会话或热更新后重复重连导致实时状态不稳定 |

## Risks / Open Questions
- 手机号无验证码存在冒用风险，后续上线前应补安全策略。
- 后台和群聊范围大，后续需要分阶段实现。
- 本地 SQLite 适合开发，线上建议迁 PostgreSQL。

## Next Step
继续实现后台核心页面，或补聊天端的文件/图片/语音/群聊能力。

## Test Notes
- 2026-08-21：和用户一起测试本地聊天页。
- 发现问题：当前打开的会话收到新消息后，左侧仍出现未读数；热更新/切会话后实时监听偶发不刷新当前消息面板。
- 修复：Socket 连接只跟当前用户生命周期绑定，使用 `activeConversationIdRef` 判断当前会话；当前会话收到消息时拉取消息并清未读，非当前会话只刷新会话列表。
- 验证：当前会话实时收到消息且不显示未读；非当前会话收到消息显示未读；`npm run check` 和 `npm run build` 均通过。
- 2026-08-21：补充图片/视频上传、多选、逐条转发、合并转发、置顶、自聊入口。
- 合并转发修正：聊天中展示“聊天记录”卡片，点击后打开记录详情弹窗，按原消息顺序展示完整内容，支持文字、图片、视频和嵌套记录占位。
- 验证：上传接口、合并转发接口、合并记录详情弹窗通过；`npm run check`、`npm run build`、`npx tsc -p apps/server/tsconfig.json --noEmit` 均通过。
- 2026-08-21：修复移动端聊天进入问题、局域网 API 地址问题、加号图标和底部功能面板。
- 移动端交互修正：列表页点联系人/会话进入聊天页，聊天页可返回；输入栏固定在聊天底部；长按消息打开操作菜单。
- 网页端交互修正：右键消息打开逐条转发、合并转发、多选菜单。
- 媒体修正：图片/视频可点击放大预览；加号面板仅保留照片、拍摄、文件。
- 验证：移动视口 390x844 下列表进聊天、返回、底部工具栏、图片放大、右键菜单均通过；`npm run check` 和 `npm run build` 均通过。
- 2026-08-21：修复输入区和工具面板交互、补充消息撤回。
- 输入区修正：点 `+` 时输入框主动失焦；点输入框时关闭 `+` 面板并清掉多选/操作菜单，避免手机键盘和扩展面板同时出现；移动端输入栏高度和底部安全区加大，更容易点。
- 多选/菜单修正：长按/右键只弹菜单，不再立刻把消息变成绿色选中；点“多选”后才进入选择态；从菜单点逐条/合并转发会自动选择当前消息。
- 撤回修正：新增 `POST /api/messages/:id/recall`，自己发送的消息可撤回；聊天区显示“你撤回了一条消息/对方撤回了一条消息”，会话列表显示 `[撤回了一条消息]`，已撤回消息不可再转发。
- 验证：接口造消息后撤回成功，`revoked_at` 写入数据库；移动视口打开自聊会话看到“你撤回了一条消息”；`npm run check`、`npm run build`、`npx tsc -p apps/server/tsconfig.json --noEmit` 均通过。
- 2026-08-21：修复手机长按消息弹出系统“拷贝/选择”菜单的问题。
- 长按修正：消息列表和气泡禁用 `user-select` 与 `-webkit-touch-callout`；移动端 `pointerdown` 长按开始时阻止默认浏览器长按行为，保留自定义微信式操作菜单。
- 验证：`npm run check`、`npm run build` 均通过。
- 2026-08-21：继续修正消息操作菜单和头像行为。
- 转发修正：黑色菜单点“逐条转发/合并转发”先进入多选状态，用户可继续勾选多条消息，再通过底部多选条执行逐条或合并转发。
- 撤回修正：自己撤回文字消息后，撤回提示旁出现“重新编辑”，点击会把原消息内容放回输入框。
- 菜单修正：操作菜单按视口宽度收紧并限制在屏幕内，避免右侧文字显示不全。
- 头像修正：自己发送的消息右侧显示头像；点击右侧自己头像进入自己的聊天界面。左侧对方头像也可点击进入对应聊天。
- 验证：`npm run check`、`npm run build` 均通过。
- 2026-08-21：修复进入聊天不自动到底、手机发送按钮点不了。
- 滚动修正：新增消息列表容器引用，进入会话/加载消息/媒体加载完成后直接把 `.message-list.scrollTop` 设置到 `scrollHeight`，避免图片视频晚加载导致还差一截。
- 发送修正：发送按钮不再依赖表单 submit，改为显式按钮点击和触屏 `touchend` 发送，并加防重入；发送后自动滚到底。
- 验证：移动视口 390x844 进入 A3 自聊后 `distanceFromBottom = 0`；输入“手机发送按钮测试”后发送按钮可用，点击后消息数量增加、输入框清空、仍在底部；`npm run check`、`npm run build` 均通过。
- 2026-08-21：继续修复真机手机发送按钮点不了。
- 真机发送修正：移除发送按钮原生 `disabled`，改用 `aria-disabled` + `.is-empty` 样式，避免 React 状态/中文输入法延迟导致触摸事件被 disabled 挡住；发送时直接读取 textarea 当前 DOM 值。
- 触摸修正：发送按钮在 `pointerdown` 和 `mousedown` 阶段就调用发送，同时保留 `click` 兜底，发送函数内部用 `sendingRef` 防重复。
- 验证：`npm run check`、`npm run build` 均通过。
- 2026-08-21：真机仍无法发送后进一步加强发送按钮。
- 发送结构修正：输入区从 `<form>` 改为普通 `<div>`，绕开移动浏览器对 form/button/键盘提交的特殊处理。
- 原生事件兜底：发送按钮增加原生 DOM `touchstart` 和 `pointerdown` 监听，外加 React `pointerdown/mousedown/click`，按钮内部增加 `.button-hitbox` 扩大命中区。
- 状态兜底：发送逻辑通过 refs 获取当前用户、当前会话和 textarea 当前值，不依赖 React 异步状态是否及时刷新。
- 验证：`npm run check`、`npm run build` 均通过。
- 2026-08-21：用户反馈 Safari 打开后仍不能发送，截图显示停留在联系人列表页。
- 诊断修正：增加 `APP_VERSION = v2026-08-21-1818` 和聊天页发送调试条，显示“等待发送/发送触发/发送成功/失败原因”，用于确认真机是否加载新版本以及点击是否进入发送函数。
- 监听修正：原生发送事件改为 callback ref 绑定，避免按钮首次未渲染时 effect 提前返回导致监听没有挂上。
- 验证：本地运行聊天页 DOM 可见版本号；`npm run check`、`npm run build` 均通过。
- 2026-08-21：根据真机调试条发现 Safari 报错 `crypto.randomUUID is not a function`。
- Safari 兼容修正：新增 `makeClientId(userId)`，优先用 `globalThis.crypto.randomUUID()`，不支持时退回 `Math.random().toString(36)` 生成 clientId；版本号更新为 `v2026-08-21-1824`。
- 验证：`npm run check`、`npm run build` 均通过。
- 2026-08-21：修复移动端聊天页返回按钮被页面滚走。
- 移动布局修正：`html/#root` 禁止页面级滚动；移动端 `.app-shell` 改为 `position: fixed; inset: 0; height: 100svh; overflow: hidden`，只允许 `.message-list` 内部滚动；切换会话时执行 `window.scrollTo(0,0)`。
- 验证：移动视口 390x844 下 `.chat-header.top = 0`、`.mobile-back.top = 11.5`、`window.scrollY = 0`、消息列表 `distanceFromBottom = 0`；`npm run check`、`npm run build` 均通过。
- 2026-08-21：修复搜索功能不生效。
- 搜索修正：聊天页按昵称、手机号、最近消息预览过滤会话；联系人页按昵称和手机号过滤联系人；搜索框增加清空按钮；空结果区分“没有找到会话/联系人”。
- 验证：浏览器测试搜索不存在关键词显示空结果，搜索 `A3` 可过滤出 A3，清空按钮可见；`npm run check`、`npm run build` 均通过。
- 2026-08-21：修复视频消息整块显示绿色。
- 媒体样式修正：自己发送的绿色气泡规则只作用于普通文字，不再作用于 `.media-bubble` 和 `.bundle-bubble`；视频消息增加 `.video-bubble` 深色背景，图片消息使用 `.image-bubble`，合并聊天记录卡回到白底。
- 验证：浏览器检查 `.video-bubble` 背景为 `rgb(21,21,21)`，`.bundle-bubble` 背景为白色；`npm run check`、`npm run build` 均通过。
- 2026-08-21：按微信默认视频消息样式优化视频卡片。
- 视频样式修正：新增 `VideoBubble`，视频消息显示缩略图容器、居中半透明播放按钮、右下角时长；根据视频方向切换横版/竖版比例；尝试用 canvas 抓首帧作为 poster，失败时仍显示浅灰占位和视频首帧，不再纯黑。
- 验证：浏览器检查 `.video-bubble` 存在播放按钮和时长，背景为浅灰 `rgb(215,221,225)`，测试视频显示时长 `0:05`；`npm run check`、`npm run build` 均通过。
- 2026-08-21：修复系统拍摄上传后还要手动点 X、视频卡片过大空白。
- 拍摄上传修正：`uploadFiles(files, source)` 增加来源参数；从系统拍摄/选择上传成功后自动 `closeCamera()` 返回聊天，并清空 capture/file input。
- 视频缩略图修正：竖屏视频卡片收小到约 `122px` 宽，横屏视频约 `220px` 宽；加载 metadata 后 seek 到 0.1 秒再抓首帧，抓不到才显示小型浅灰“视频”占位。
- 验证：浏览器测试横屏视频卡片为 `220x124`，存在 `has-poster` 和时长；`npm run check`、`npm run build` 均通过。
- 2026-08-21：完成后台第一版。
- 后台范围：新增 `/admin` 管理页，使用本地固定密码 `admin123456`；支持查看用户统计、搜索用户、启用/禁用用户、编辑自动回复开关和文案。
- 自动回复：新用户首次手机号登录后，由 A1 自动创建会话并发送后台配置文案；支持 `{nickname}` 变量，实测 A7 收到 `A7 你好抖音评论...`。
- 用户管控：A1 是自动回复账号，后台显示“保留”且禁止禁用；其他禁用用户会被后端拦截登录、拉取用户/会话/消息、创建会话、发消息、上传、置顶、撤回、转发等操作。
- 验证：后台页面登录成功，自动回复保存后可恢复；禁用 A2 后登录被拦截并恢复启用；`npm run check`、`npm run build`、`npx tsc -p apps/server/tsconfig.json --noEmit` 均通过。
- 2026-08-21：后台升级为三层权限和链接归属。
- 权限设计：固定超级管理员账号 `admin`，初始密码 `000000`；超管可创建多个管理员，管理员可创建客服；管理员只能看自己名下客服和链接，超管看全局。
- 编号设计：管理员账号设置客户编号前缀，例如 `aa`；该管理员名下客服链接进入的新客户按 `aa1、aa2...` 递增。后端校验编号前缀唯一，重复时返回“客户编号前缀已存在”，前端弹框提示。
- 链接设计：邀请链接绑定客服，每条链接有独立自动回复文案；客户通过 `/?invite=CODE` 进入后仍填手机号，首次登录会分配到该客服，并发送该链接话术。
- 验证：创建小明管理员前缀 `aa8053`、创建小明客服、创建链接后，新客户通过链接进入生成 `aa80531`，会话归属客服 `A8`，自动回复为链接专属文案；重复前缀被拦截。`npm run check`、`npm run build`、`npx tsc -p apps/server/tsconfig.json --noEmit` 均通过。
- 2026-08-21：根据用户反馈简化后台，取消普通管理员层级。
- 新后台结构：固定超级管理员 `admin / 000000` 直接管理客服、链接/二维码、客户和全局回复；后台侧边栏只保留“客服 / 链接/二维码 / 客户 / 全局回复”。
- 新编号规则：编号前缀放在客服上，例如客服设置 `bb`，该客服链接进来的客户按 `bb1、bb2...` 自增；前缀重复仍弹框提示“客户编号前缀已存在”。
- 二维码：链接列表改为卡片式，直接显示二维码、入口 URL、客服、访问数、客户数和停用/启用按钮。
- 验证：创建客服前缀 `bb5790`，重复前缀被拦截；创建链接后新客户通过链接进入生成 `bb57901`，自动回复为链接专属文案，会话归属客服 `A10`；后台页面只显示客服和链接/二维码结构；`npm run build` 和服务端 `tsc --noEmit` 通过。
- 2026-08-21：后台从单页长滚动改为模块页。
- 页面结构：左侧“客服 / 链接/二维码 / 客户 / 全局回复”改为内部状态切换，不再使用 `#links` 这类 hash 路由；右侧只渲染当前模块，避免客服、链接、回复、客户都堆在一个页面。
- 验证：浏览器点击“链接/二维码”后 URL 保持 `/admin`，页面只显示链接模块；点击“客户”后只显示客户模块；`npm run check`、服务端 `tsc --noEmit` 和 `npm run build` 通过。
- 2026-08-21：修正客服账号和分页。
- 客服规则：客服不是客户，不使用 A 编号或客服前缀自增作为显示名；后台填写客服名称如“小路”，聊天界面和联系人显示“小路”。旧客服聊天身份会自动同步为客服名称。
- 登录规则：聊天端登录页增加“客户 / 客服”切换；客服用后台创建的账号和密码直接登录聊天端，不需要手机号，默认密码可设为 `000000`。
- 后台表单：添加客服输入顺序调整为“客服名称 / 登录账号 / 客户编号前缀 / 初始密码”，更符合使用顺序。
- 分页规则：后台客户页增加分页，当前每页 10 位客户，显示总数、当前页、上一页和下一页。
- 验证：客服 `svc5790` 登录聊天端后昵称为“测试客服”，后台客服表聊天显示也为“测试客服”；客户页显示分页条“共 9 位客户 · 第 1 / 1 页”；`npm run check`、服务端 `tsc --noEmit` 和 `npm run build` 通过。
- 2026-08-21：增加删除操作。
- 聊天端删除好友：聊天页头部增加“删除好友”按钮，点击前弹确认；确认后仅对当前用户隐藏该好友和直接会话，属于软删除，不物理删除消息数据。
- 后台删除：客服列表和链接/二维码卡片增加“删除”按钮，点击前弹确认；删除客服会禁用客服账号并隐藏，删除链接会停用并隐藏，均为软删除。
- 验证：接口创建临时客服/链接后删除，后台列表不再返回；删除好友后联系人列表不再返回该好友；浏览器看到客服和链接删除按钮；`npm run check`、服务端 `tsc --noEmit` 和 `npm run build` 通过。
- 2026-08-21：调整聊天列表置顶/删除交互。
- 列表交互：聊天列表支持网页端右键会话弹出“置顶/删除”菜单；手机端长按会话弹出同样菜单；手机端会话行左滑露出“置顶/删除”按钮。
- 置顶样式：置顶会话使用微信感浅灰底，当前验证颜色约为 `rgb(223, 230, 235)`；置顶排序仍沿用后端 `is_pinned DESC`。
- 删除逻辑：列表删除复用软删除好友接口，删除后当前用户的联系人和会话列表隐藏该好友。
- 验证：浏览器右键会话菜单显示“置顶删除”，点击置顶后置顶行变灰且 `pinnedCount = 1`；`npm run check`、服务端 `tsc --noEmit` 和 `npm run build` 通过。
- 2026-08-21：修复桌面端会话滑动按钮误显示。
- 修正：桌面端不再显示左滑操作按钮，也不再让会话行保持滑动位移；桌面端只保留右键菜单操作。手机端才启用滑动露出“置顶/删除”。
- 修正：聊天详情头部去掉“删除好友”按钮，避免和列表删除逻辑重复。
- 验证：浏览器桌面端 `.swipe-actions` 为 `display:none`，会话行 transform 为 `none`，详情头部不含“删除好友”；`npm run check`、服务端 `tsc --noEmit` 和 `npm run build` 通过。
- 2026-08-21：修复编号头像和客服联系人范围。
- 头像规则：聊天端头像不再统一截取前两个字符；英文/数字编号显示最多 4 位，所以 `cc1`、`aa12` 会完整显示，中文名称仍显示前两个字。
- 联系人规则：客服登录聊天端后只显示自己负责的客户，不再显示全部历史客户和其他客服；客户登录后只显示自己归属的客服，未通过链接分配的客户默认显示 A1。
- 验证：数据库检查 `xiaolu` 只绑定客户 `cc1`；`npm run check`、服务端 `tsc --noEmit` 和 `npm run build` 均通过。
- 2026-08-21：区分客户和客服头像颜色。
- 头像颜色：客户保持淡绿色，客服/超级管理员使用淡蓝灰；按账号身份判断，`staff:` 和 `admin` 归为客服色，手机号注册用户归为客户色。
- 覆盖范围：顶部自己的头像、会话列表、联系人列表、聊天消息左右头像、转发弹框头像。
- 验证：`npm run check` 和 `npm run build` 均通过。
- 2026-08-21：修复客服聊天列表仍显示未归属历史会话。
- 会话范围：客服聊天列表现在只显示自聊和分配给该客服的客户会话；客户聊天列表只显示自聊和自己的专属客服，未通过链接分配的客户默认只显示 A1。
- 权限补强：创建/打开私聊时也校验归属关系，不能再通过历史数据或接口打开未分配客户/客服。
- 验证：数据库中 `xiaolu` 仍有 A1 历史会话，但 `getConversations(14)` 过滤后只返回“小路”和 `cc1`；`npm run check`、服务端 `tsc --noEmit` 和 `npm run build` 均通过。
- 2026-08-21：取消未匹配客户的 A1 兜底。
- 登录规则：未通过专属链接/二维码绑定客服的客户直接返回 `未匹配专属客服`，前端清空登录态并退出，不再默认显示 A1。
- 会话规则：`getUsers`、`getConversations` 和 `getOrCreateDirectConversation` 都改成硬校验归属，避免把消息发到错误对象。
- 验证：`npm run check`、服务端 `tsc --noEmit` 和 `npm run build` 均通过。
- 2026-08-24：修复删客服后的链接联动和专属客服匹配。
- 删除客服：现在会同步停用该客服名下全部邀请链接，不再出现“客服删了，链接还在但删不掉”的情况。
- 删除链接：服务端改成对客服归属做软校验，管理员删自己/超管删全部都能正常执行。
- 登录校验：保留“未匹配专属客服”的硬拦截，但新用户首次通过有效链接进入时仍会先分配客服，再创建账号，不会误挡专属链接用户。
- 验证：`npm run check`、服务端 `tsc --noEmit` 和 `npm run build` 均通过。
- 2026-08-24：修复旧归属挡住新链接的问题。
- 归属替换：如果用户原来绑定的客服已被删除/失效，再通过新的有效链接进入时会自动改绑到当前链接客服。
- 登录规则：带邀请码时优先走链接分配，不再因为旧归属残留提前弹“未匹配专属客服”。
- 验证：`npm run check`、服务端 `tsc --noEmit` 和 `npm run build` 均通过。
- 2026-08-24：实测修复了“链接进入仍弹未匹配专属客服”。
- 验证路径：手机号 `17606505651` 通过有效链接 `LMT6KZPK2VLOPZ` 登录成功，`/api/users?userId=15` 返回客服 `wj`，`/api/conversations?userId=15` 返回自聊和对应会话，数据库归属已改到当前链接客服。
- 2026-08-24：继续修复“通过链接进入后要直接打开客服聊天界面”。
- 登录结果现在会返回 `openConversationId`，前端登录后会优先把当前会话切到专属客服，不再停留在空列表或自聊页。
- 验证：接口测试新手机号 `17606501176` 通过链接后返回 `openConversationId=17`，会话为 `wj`；浏览器实测新手机号 `17606627039` 登录后右侧标题直接为 `wj`，左侧选中 `wj`，输入框可见，自动回复进入该会话。
- 2026-08-24：开始补客服头像和客户备注能力。
- 数据层新增 `users.avatar_url`、`staff_accounts.avatar_url` 和 `customer_assignments.remark_name`，用于客服头像和客服侧客户备注。
- 前端开始把头像渲染改成图片优先、文字兜底，并给后台客服表留出头像上传入口。
- 2026-08-24：新增客服前台快捷语。
- 快捷语规则：只给客服聊天端显示，客服在聊天输入区旁点击“快捷语”打开右侧快捷语栏；快捷语由客服自己增删改，后台/数据库保存同步，每个客服互相隔离。
- 快捷语交互：不需要标题，前台只填写回复内容；点击快捷语只插入输入框，不自动发送，客服可以再修改后发送。
- 移动适配：手机端快捷语面板从底部弹出，避免右侧栏挤压；手机会话列表调整为单栏微信式结构，左滑按钮固定宽度，避免时间和删除按钮混在一起。
- UI 修正：移除聊天输入区上方开发调试条，正常聊天界面不再显示版本/发送调试文字。
- 验证：快捷语前台新增、插入、编辑、删除通过；接口创建/查询/删除通过；`npm run check`、`npm run build`、`npx tsc -p apps/server/tsconfig.json --noEmit` 均通过。
- 2026-08-24：按企业微信参考继续修正快捷语栏。
- 桌面布局：快捷语栏从聊天面板内部挪到主布局第三列，打开时是“聊天区右侧外展”，不再覆盖聊天内容；收起后保留 48px 窄栏入口。
- 快捷语发送：每条快捷语增加“发送 / 编辑 / 删除”，点“发送”直接发给当前会话用户，不写入输入框，发送后快捷语栏保持打开。
- 样式修正：右侧栏宽度固定为 360px，列表禁横向滚动，发送按钮保留明文“发送”，快捷语卡片改为纵向内容 + 操作按钮，避免文字和按钮挤压。
- 验证：浏览器实测聊天区与快捷语栏不重叠，发送按钮文字存在；直接发送后消息出现在聊天记录，输入框保持空，面板保持打开；收起/展开正常；`npm run check`、`npm run build` 均通过。
- 2026-08-24：修复快捷语按钮文字颜色和增加拖拽排序。
- 样式修正：底部“快捷语”按钮和快捷语卡片里的“发送”按钮都强制绿色底白字，不再被通用按钮 hover/active 样式覆盖；浏览器 computed style 验证文字为 `rgb(255,255,255)`。
- 拖拽排序：快捷语卡片左侧新增拖拽把手，拖动到目标快捷语上会立即保存顺序；新增 `PATCH /api/quick-replies/reorder/list` 保存每个客服自己的排序。
- 端口修正：已停掉旧 5173/5174 前端残留进程，最新 Vite 服务固定跑在 `http://localhost:5173/`。
- 验证：`npm run check`、`npm run build`、服务端 `tsc --noEmit` 通过；浏览器验证快捷语按钮文字、发送按钮文字、拖拽把手、右栏不重叠均通过。
- 2026-08-24：登录、品牌名和后台客户批量操作更新。
- 登录限制：客户手机号登录前后端都限制为 11 位纯数字；前端输入框会过滤非数字并截断到 11 位，后端 `/api/login` 只接受 `^\d{11}$`。
- 品牌名：前台登录页从“本地聊天”改为“抖抖IM”，后台侧栏从“本地聊天后台”改为“抖抖IM后台”。
- 后台客户批量操作：客户列表增加多选框、当前页全选、批量禁用、批量启用、批量删除；删除为软删除，客户会禁用并从后台列表隐藏，不物理删除聊天记录。
- 数据库：`users` 增加 `deleted_at` 字段用于软删除客户；后台客户统计和列表自然排除已删除客户。
- 验证：`npm run check`、`npm run build`、服务端 `tsc --noEmit` 通过；接口验证 10/12 位手机号被拒绝，批量禁用后恢复通过；浏览器验证输入 14 位只保留前 11 位、后台批量操作条出现。
- 2026-08-24：为 Cloudflare Tunnel 临时外网访问做准备。
- 前端 API 地址从固定 `当前域名:4000` 改为 `window.location.origin`，避免外网 HTTPS 链接无法访问本地 4000 端口。
- 新增 Vite 代理配置：`/api`、`/uploads`、`/socket.io` 都从 `5173` 代理到本地后端 `4000`，Cloudflare 只需要代理 `http://localhost:5173`。
- 页面标题同步改成 `抖抖IM`。
- 验证：`5173/api/admin/login` 通过代理登录成功；`5173/socket.io/?EIO=4&transport=polling` 返回 Socket.IO 握手；`npm run check`、`npm run build`、服务端 `tsc --noEmit` 均通过。
- 2026-08-24：增加轻度区分开发和临时线上。
- 开发环境继续使用 Vite dev：`http://localhost:5173`，用于日常修改和热更新测试。
- 临时线上使用 Vite preview：`http://localhost:4173`，由 `npm run preview:web` 启动，Cloudflare Tunnel 后续应指向 `http://127.0.0.1:4173`。
- `4173` 预览服务同样代理 `/api`、`/uploads`、`/socket.io` 到后端 `4000`，保持登录、上传和实时聊天可用。
- 验证：`4173` 页面返回 200，`4173/api/admin/login` 登录成功，`4173/socket.io/?EIO=4&transport=polling` 握手成功；`npm run check`、`npm run build`、服务端 `tsc --noEmit` 均通过。
- 2026-08-24：修复线上体验反馈。
- 上传反馈：图片/视频上传时聊天区显示“正在上传...”，按钮避免重复触发，上传成功后主动刷新当前会话。
- 会话切换：会话点击统一走 `openConversation`，先清空旧消息并显示“正在切换会话...”；消息加载带序号校验，慢请求返回时如果已切到别的会话会被丢弃，避免标题和内容串屏。
- 客服头像：会话、联系人和消息查询都用 `staff_accounts.avatar_url` 兜底，后台配置的客服头像在聊天端可显示。
- 文字复制：桌面端聊天文字恢复 `user-select: text`；移动端仍保留禁用系统长按复制，避免影响自定义长按菜单。
- 新客户昵称：以后新手机号客户不再按客服前缀自增显示名，改为 3-5 位 `a-z` 随机小写字母，内部 `sequence_number` 继续保留用于排序。
- 验证：新客户通过链接登录得到随机昵称 `qiyq` 且符合 `^[a-z]{1,5}$`；客服消息返回后台头像 URL；上传图片接口成功并返回头像；`npm run check`、`npm run build`、服务端 `tsc --noEmit` 均通过。
